[project]
name = "rllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10.10"
dependencies = [
    "python-binance>=1.0.32",
    "torch>=2.9.0",
]
