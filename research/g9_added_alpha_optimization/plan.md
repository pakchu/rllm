# G9 plus additional-alpha allocation plan

1. Reconstruct the five authoritative G9 sleeves with committed runtime signal adapters and frozen Rank7 model; do not replace missing REX or blocked model input with a zero sleeve.
2. Add macro-flow/regime switch, OI divergence pullback, and regional trend on the same June1--Sep5 00:00 UTC 5m ledger. Compare actual market grid and prices before mixing.
3. Express weights in notional/equity units. G9 historical 0.5x base sleeve convention gives [1,1.5,.2,.8,1] = 4.5; repository-live G8 control gives [1,1,.2,.8,1] = 4.0. Preserve both named controls.
4. Allow sleeve overlap. Net signed BTC units before fees/funding and enforce 4.5x net cap at execution events; no gross-overlap/frequency/fee-ratio veto. Gross allocation search remains bounded, not claimed exhaustive.
5. Freeze seeded allocation candidates before economics; June selects by cost-stressed Calmar with insolvency rejection. Report fixed finalists July--September4 plus September-only, no subsequent-period rerank. Explicit retrospective diagnostic is separate if produced.
6. Barrier exits retain exact prices and fixed-unit holds. Evaluate conservative MDD including unhedged exposure after either side of an offset exits intrabar. State timing/latency/liquidation limits.
7. Regression tests and independent review before claiming completion. Preserve all failures, source hashes and disabled-only output. Never alter live configurations.

All windows have been exposed. This is not pristine OOS; a high score does not authorize live use.
