# HVSOF-8 ordered state-filter battery preregistration

After two exact-event AND batteries proved sparse, HVSOF-8 freezes a different
combination grammar without altering any component. Three immutable action clocks
(HVRSSR-8 liquidity reversal, HVTFR-8 path-trend continuation, HVSVF-8 signed
variance feedback) are each filtered by one of two broad, frozen state
subconditions: HVTCCR concentration rank >=0.80 or HVLZC complexity rank <=0.25.

The filter has no side and cannot alter action direction, entry, 8h hold,
variation gate, onset, or reservation. The family is exactly six ordered
action-by-state candidates. Source support and unchanged Gross9 precede train-only
raw-rank-one selection. Bonferroni uses all six hypotheses (`raw p<=0.10/6`), and
failure terminates without fallback or repair.

All standalone and earlier combination outcomes are known; this remains
exploratory discovery. No HVSOF incidence or outcome was opened before the lock.
The preregistration and five-test suite reproduced twice. SHA-256:
`53e78737a357eb183c8247bbca847a8319334400be5390e4bf01861a368e3484`.
