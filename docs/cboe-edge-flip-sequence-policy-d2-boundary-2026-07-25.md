# CEFS-D2 Cboe Edge-Flip Sequence Policy boundary

Date: 2026-07-25

Status: **frozen before any CEFS-D2 source value, source-support incidence,
market outcome, reward, model output, action, or trade is opened.**

## Identity

```text
policy_id
  CEFS-D2
name
  Cboe Edge-Flip Sequence Policy — clean-room successor
purpose
  recover only from CEFS-D1's pre-source operator-shell authority failure
```

CEFS-D1 remains retired and may never be rerun:

```text
rejection
  results/cboe_edge_flip_sequence_policy_source_rejection_2026-07-25.json
SHA256
  4c2839e5ac59738367d5116ff05ed50c900d16f06de8c4d4cc724fd25978c169
result_hash
  9963981f6d56fcff65f1367fc7c3c1fc006b60b821894b3e0ff59c6b9aa35d7b
```

The D1 failure was `FileNotFoundError: git` at Gate 1 after zsh's reserved
`path` variable erased `PATH`. The source loader was not entered and every
forbidden counter remained zero.

## Decision authority

```text
successor decision
  docs/cefs-d2-successor-decision-2026-07-25.md
commit
  b0061fe4c944eefdbdd5e72139e3765b2fd464f1
SHA256
  60a9f9b01a1943f9a3079885c36ac9af2bfe4686a28e206c8198b36880211aea
```

## Byte-bound scientific authority

CEFS-D2 imports its entire scientific mechanism unchanged from:

```text
D1 boundary
  docs/cboe-edge-flip-sequence-policy-boundary-2026-07-25.md
commit
  b7297ccf9235da98edcbe9b35d84009245972321
SHA256
  d0b522a7ac87e3526d6cd740bb81304bd73042bc327978660eb551b159c16ec3

D1 preregistration artifact
  results/cboe_edge_flip_sequence_policy_preregistration_2026-07-25.json
commit
  d60d9744e82f48350be506f1da63bcb2e706cf03
SHA256
  5e515663e99ef4aa322cae25cfb2c07f69b3e24f289bc2f0f79463aca64a8878
manifest_hash
  9aa7c891ec241d4733db215068bed3507f41c03cbae7198c906a079ddb6467bf

D1 preregistration producer
  training/preregister_cboe_edge_flip_sequence_policy.py
commit
  ec8eae23226d39f0c62b6c5711d6080f2bf990a4
SHA256
  1c4668e72846eadf66011c582c62e8574af3679204500c1ff9631101ecbb7ac1

pure support engine
  training/build_cboe_edge_flip_sequence_policy_support.py
commit
  d7213f647128fc6160672bc61f080b3dcf7d1f42
SHA256
  2069084d65146540488672115ee09f292cd31e6611bf92a569d534ab8a74c688
```

The D2 runner may import pure parsing, edge, sequence, schedule, control,
metric, replay, deterministic serialization, and terminal semantic-validation
functions from that exact engine. It may not call D1 `run_official`, use any
D1 terminal path, or alter the imported module.

## Exact unchanged mechanism

The following are inherited byte-exact from the D1 preregistration:

- all three Cboe source paths, hashes, manifests, audits, and headers;
- source horizon `[2020-01-01, 2024-01-01)`;
- exact common-date intersection and VIX identity requirement;
- twelve ordered primitive relation formulas;
- `LOWER|EQUAL|HIGHER` edge levels;
- `EARLIEST|EARLY|MIDDLE|LATE|CURRENT` state order;
- exact three current-target prompt templates;
- `TARGET_LONG|TARGET_FLAT|TARGET_SHORT`;
- D+1 09:30 New York availability;
- D+1 09:35 New York entry;
- exact 288 five-minute bar hold;
- global non-overlap reservation with equality accepted;
- TRAIN 2020–2021, TEST 2022, EVAL 2023;
- all source-support counts, distribution limits, drift limits, and controls;
- all deterministic prefix and synthetic-append replay rules; and
- every forbidden-access counter and no-repair rule.

No source-owned side, score, thresholded event, vote, rank, percentile,
selected date subset, raw number, date, return, or hidden reasoning may enter
the model prompt.

## Runtime authority hardening

This is the only substantive implementation difference:

```text
git executable
  /usr/bin/git
SHA256
  2a8c18fbf43da9f692d75474c72bea9dfd796c260b0f3dfe456376abc3bbd668
```

Before source access the runner must:

1. require a nonempty `PATH`;
2. require `/usr/bin` as an exact `PATH` component;
3. validate `/usr/bin/git` as a regular non-symlink file;
4. validate its exact SHA256;
5. execute `/usr/bin/git --version` successfully;
6. use `/usr/bin/git` for every Git authority operation;
7. validate the D2 boundary, preregistration, contract, runner, tests, seal,
   and exact D1 dependencies; and
8. require a clean worktree and absent D2 terminal/pass artifacts.

Executable-name lookup is forbidden. A shell preflight variable named `path`
is forbidden.

## D2 artifacts

```text
preregistration
  results/cboe_edge_flip_sequence_policy_d2_preregistration_2026-07-25.json
execution seal
  results/cefs_d2_source_support_execution_seal_2026-07-25.json
pass source
  data/cboe_edge_flip_sequence_policy_d2_source_2020_2023.csv.gz
pass controls
  data/cboe_edge_flip_sequence_policy_d2_controls_2020_2023.csv.gz
pass report
  results/cboe_edge_flip_sequence_policy_d2_source_support_2026-07-25.json
rejection report
  results/cboe_edge_flip_sequence_policy_d2_source_rejection_2026-07-25.json
```

All paths are create-once. D1 paths are read-only authorities and cannot be
used as D2 outputs.

## Source-support gates

Gate order and check formulas are exactly D1:

1. runtime, authority, and forbidden access;
2. schema and chronology;
3. schedule support and full schedule replay;
4. primitive edge support;
5. state diversity and role-level stability;
6. source-only controls;
7. deterministic build, physical-prefix replay, and synthetic append replay.

At the first failed gate, stop and write only the D2 rejection report.

Exact failure action:

```text
retire_cefs_d2_unchanged_before_outcomes
```

Only a complete seven-gate pass writes D2 source/control outputs and the pass
report.

Exact pass action:

```text
authorize_cefs_d2_economic_rllm_evaluator_freeze_only
```

## Outcome boundary

Before a source-support pass the runner may not open or build:

- BTC price or execution rows;
- funding;
- post-2023 Cboe source values;
- future returns or labels;
- rewards;
- model rows or selected actions;
- trades;
- comparator actions; or
- PnL, CAGR, MDD, Sharpe, or any economic metric.

The D2 preregistration artifact must show `source_values_opened=false` and
`outcomes_opened=false`. The execution seal must bind exact committed runner
and test bytes before the one official source run.

## One-shot and no-repair rule

CEFS-D2 receives one official source-only invocation. A terminal report is
idempotent and immutable. Any failure retires D2 unchanged.

Do not create D3 to adjust a source field, threshold, clock, sequence,
control, gate, role, hold, or date window after observing D2 incidence.
Only an implementation-independent new mechanism may follow a scientific D2
failure.
