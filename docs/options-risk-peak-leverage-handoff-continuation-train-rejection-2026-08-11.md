# ORPLHC-6 train rejection — 2026-08-11

The unchanged candidate is terminally rejected at train. Base return is `-5.7674%`, stress return is `-6.8561%`, mean gross underlying move is `-28.7207bp`, and the one-sided weekly cluster sign-flip p-value is `0.9765`. Both calendar halves are negative (`-3.0034%` and `-2.8496%`). Strict MDD is `6.7205%`, but all return, efficiency, gross-edge, significance, stress, and half-consistency requirements fail.

No later stage was opened. Direction, threshold, clock, hold, subset, source, and diagnostics cannot be repaired or promoted.
