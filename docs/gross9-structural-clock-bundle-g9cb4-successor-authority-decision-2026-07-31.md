# Gross9 structural clock bundle G9CB-4 successor authority decision — 2026-07-31

## Decision

Freeze `G9CB-4` as a new candidate-independent structural-clock
infrastructure identity.

`G9CB-4` is not a retry, resume, repair, amendment, v2, or completion of
`G9CB-3`. The single official `G9CB-3` production invocation published its
attempt-consumed sentinel and pass-1 capability-consumed ledger, then failed
terminally because the builder incorrectly required the exclusive
half-open-domain end to exist as a physical market value-row open.

The `G9CB-3` sentinel and pass-1 ledger are immutable ignored/untracked
terminal evidence at the parent `C3`. They must be sealed together at `T3`.
Neither file, separately or together, is `D3`, a clock authority, or a
successful publication.

This decision inherits without alteration the candidate-independent economic
object and every compatible mechanic frozen by:

```text
docs/gross9-structural-clock-bundle-authority-decision-2026-07-31.md
docs/gross9-structural-clock-bundle-rank7-authority-amendment-2026-07-31.md
docs/gross9-structural-clock-bundle-runtime-isolation-amendment-2026-07-31.md
docs/gross9-structural-clock-bundle-preregistration-correction-amendment-2026-07-31.md
docs/gross9-structural-clock-bundle-successor-authority-decision-2026-07-31.md
docs/gross9-structural-clock-bundle-g9cb3-successor-authority-decision-2026-07-31.md
```

The exact signal, model, source, domain, sleeve, weight, side, hold, barrier,
feature, schedule, Rank7 learner and source-routing, environment, static
closure, isolated-runtime, two-pass, capability, counter, serialization,
publication, manifest-last, candidate-independence, economic-prohibition, and
overlap-prohibition semantics remain frozen except for the boundary
representation correction stated here.

Only these successor changes are authorized:

1. the new `G9CB-4` identity and v1-under-`G9CB-4` protocol versions;
2. the exact `C3 -> A4 -> T3 -> Q4 -> P4 -> C4 -> D4` history;
3. one atomic terminal-evidence seal containing both existing immutable
   `G9CB-3` evidence files;
4. a widened failed-predecessor schema that requires and authenticates both
   evidence files as one indivisible predecessor finding;
5. the correction that separates physical value-row opens from a derived
   300-second boundary vector;
6. the exact tests required to prove that correction without opening official
   source values;
7. identity-specific path, exception, action, bytecode-prefix, and stage-prefix
   names; and
8. the exact active-authority, topology, and history bindings required by the
   new identity.

This decision is not operative merely because this file exists. It must be
committed and pushed alone as `A4` while `HEAD` is the exact clean pushed
`C3`. Only then may the two existing terminal-evidence files be force-added
together as `T3`.

## Evidence boundary and confidence

This decision distinguishes authenticated artifact facts, supplied execution
observations, and derived root-cause conclusions.

### Authenticated artifact facts

The following facts are reproducible from current filesystem and Git metadata
without reading an official source-value file:

- `C3` is the clean pushed commit
  `4365522eba5b01129c6aa12a5e06d314b611c840`;
- the `G9CB-3` authority, preregistration, and access claim are tracked at
  their exact commits, modes, SHA-256 values, and Git blobs below;
- the ignored/untracked sentinel and pass-1 ledger reproduce their exact
  canonical bytes, sizes, modes, SHA-256 values, and prospective Git blobs;
- the pass-1 stage is an empty mode-`0700` directory;
- the pass-2 stage is absent;
- the pass-2 ledger, canonical CSV, and final manifest are absent; and
- no pass receipt, per-pass core, staged CSV, or fixed bytecode-cache path
  exists.

The sentinel's `access_at_publication` zeros and the ledger's
`consumed_before_runtime_or_value_access` status describe their publication
moments only. They do not prove that the worker remained at zero access after
the ledger was durably published.

### Supplied execution and forensic facts

The single official `--produce` invocation:

- ran exactly once;
- terminated with status `1` after approximately `59.35` seconds;
- reached maximum resident set size `1,352,228` KB;
- produced empty stdout;
- published the sentinel and the pass-1 ledger;
- consumed only the pass-1 capability;
- imported both declared isolated runtime roots;
- fully decoded and handed off market, funding, premium, and open-interest
  inputs;
- did not open the REX JSONL inputs or Rank7 model/history inputs;
- did not reach feature construction, schedules, sleeve intervals, candidate
  or comparator access, pre-2025 anchor access, economics, or overlap; and
- terminated with the worker message
  `bound market lacks the canonical domain-end boundary row`, followed by the
  exact parent message
  `fresh worker failed with PID 2087327 and status 1`.

The exact decoded row counts and exact handed-off row sets are not recoverable.
No successor artifact may invent them. The G9CB-4 predecessor disclosure must
therefore state the known categorical exposure and explicitly mark exact
counts as unrecoverable.

The `/tmp` stdout and stderr/time captures are diagnostic observations, not
durable artifacts or future inputs. Their hashes and sizes bind the stated
diagnostic finding but do not authorize reading them during production.

### Root-cause conclusion

The terminal message and the half-open domain contract establish the
representation error. Parent authentication separately proves that the
source bytes matched their preregistered hashes at authentication time:

```text
DOMAIN_END = 2026-06-01T00:00:00Z
```

is an exclusive closing boundary, not a physical value-row open. The builder
wrongly required that boundary to occur in the market value timestamp array.
The open-interest attachment is a left merge and cannot remove market rows.
The parent authenticated the source hashes before access, so a hash mismatch
at authentication time is ruled out. Authentication and later decode use
separate opens; this decision does not claim that a concurrent
post-authentication mutation was impossible. The traceback directly proves
that the decoded working frame lacked a physical `DOMAIN_END` row. No source
mutation, source replacement, fabricated row, or domain change is authorized
by this conclusion.

The confidence in the failure classification and correction is high. Exact
post-ledger row counts remain unknown and must remain disclosed as unknown.

## Immutable G9CB-3 terminal finding

The exact predecessor chain is:

```text
A3 = a97576c050cf7cdf08738ddb755e63cc92484428
T2 = edad4de5cf5524c4646c64b0581e47c914e31425
Q3 = 211752bf2529b37376b5fecdc190e0c22a1195ff
P3 = 7f4985b0685fdb6cbbde891485f08305f076e205
C3 = 4365522eba5b01129c6aa12a5e06d314b611c840
```

The operative `G9CB-3` authority is:

```json
{
  "authority_commit": "a97576c050cf7cdf08738ddb755e63cc92484428",
  "git_blob": "43d68f6b7407c19b3b52ef8b7bb7010797dbf3b3",
  "git_mode": "100644",
  "path": "docs/gross9-structural-clock-bundle-g9cb3-successor-authority-decision-2026-07-31.md",
  "path_type": "regular_file",
  "sha256": "1df555c5149bfe269d2cc2c87375d54032809f13ac36f4e92b5ba00dd6e87cc7"
}
```

The operative `G9CB-3` preregistration is:

```json
{
  "filesystem_mode_octal": "0444",
  "git_blob": "e4b2488783f0cfa88a8478b3ff0d94c99fbece6e",
  "git_mode": "100644",
  "manifest_hash": "e100e54557a9654923374dec433feff494d85b0956129157231c2fb8551ec5f2",
  "path": "results/gross9_structural_clock_bundle_g9cb3_preregistration_2026-07-31.json",
  "path_type": "regular_file",
  "protocol_implementation_commit": "211752bf2529b37376b5fecdc190e0c22a1195ff",
  "protocol_version": "gross9_structural_clock_bundle_g9cb3_preregistration_v1",
  "seal_commit": "7f4985b0685fdb6cbbde891485f08305f076e205",
  "sha256": "1d967a590e50822dd83c20838c8796523d6594f0fcbd22aad137b0a97c1a982c",
  "size_bytes": 34265
}
```

The operative `G9CB-3` access claim is:

```json
{
  "claim_hash": "886fa3b177cd6d48c630020c2afef7d85b05126fd3805676bffdbec2899f2c6a",
  "filesystem_mode_octal": "0444",
  "git_blob": "e4464c15f103c760a89ada18b0e49f7a874d8578",
  "git_mode": "100644",
  "path": "results/gross9_structural_clock_bundle_g9cb3_access_claim_2026-07-31.json",
  "path_type": "regular_file",
  "protocol_parent_commit": "7f4985b0685fdb6cbbde891485f08305f076e205",
  "seal_commit": "4365522eba5b01129c6aa12a5e06d314b611c840",
  "sha256": "6d59505ef14ce92bf5958d32725b7e5e1ffe8ce4065f215fdd092176a44bbe98",
  "size_bytes": 14065
}
```

The single official command was invoked exactly once:

```text
PYTHONPATH="$PWD" PYTHONDONTWRITEBYTECODE=1 \
  /usr/bin/time -v uv run python -B \
  -m training.build_gross9_structural_clock_bundle --produce
```

Captured stdout was empty:

```json
{
  "sha256": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
  "size_bytes": 0
}
```

The diagnostic stderr/time capture was:

```json
{
  "sha256": "6c3f838c364acda081f576f3d379c05c71ff8cce3c8424bb8de5c59d9e7e8d88",
  "size_bytes": 2894
}
```

The durable attempt sentinel is:

```json
{
  "claim_commit": "4365522eba5b01129c6aa12a5e06d314b611c840",
  "filesystem_mode_octal": "0444",
  "git_blob": "672a973ae09495d38253991df90c7962c6f9c2ee",
  "git_mode": "100644",
  "manifest_hash": "aab875ecc91a07619b3006967efca42664cc77d90d0bb9e45da20e93ea16c973",
  "parent_authentication_sha256": "b93873825f3598c3c04da1ddec60ea8346742872c79800ba38cc5f41906ca4c3",
  "path": "results/gross9_structural_clock_bundle_g9cb3_attempt_consumed_2026-07-31.json",
  "path_type": "regular_file",
  "protocol_version": "gross9_structural_clock_bundle_g9cb3_v1",
  "resume_allowed": false,
  "retry_allowed": false,
  "sha256": "7cec9807503c271e993f8bf456ca772d7ded9cec9619d37cbd4ac015d14c9f69",
  "size_bytes": 3300,
  "status": "attempt_consumed_before_runtime_or_value_access"
}
```

The durable pass-1 capability ledger is:

```json
{
  "carrier_device": 14,
  "carrier_inode": 7485375,
  "carrier_kind": "anonymous_pipe_v1",
  "claim_hash": "886fa3b177cd6d48c630020c2afef7d85b05126fd3805676bffdbec2899f2c6a",
  "filesystem_mode_octal": "0444",
  "git_blob": "0e77d77fcdc70379d94a2c34e8dee19f9f45d0a9",
  "git_mode": "100644",
  "identity": "G9CB-3",
  "parent_pid": 2087007,
  "path": "results/gross9_structural_clock_bundle_g9cb3_worker_capability_consumed_pass1_2026-07-31.json",
  "path_type": "regular_file",
  "preregistration_manifest_hash": "e100e54557a9654923374dec433feff494d85b0956129157231c2fb8551ec5f2",
  "protocol_version": "gross9_structural_clock_bundle_g9cb3_v1",
  "sentinel_manifest_hash": "aab875ecc91a07619b3006967efca42664cc77d90d0bb9e45da20e93ea16c973",
  "sha256": "45b27b24074c53eeaaa9a1074c7888c22c4078e5fb5922007d31ad0baf704f66",
  "size_bytes": 1766,
  "slot": 1,
  "stage_directory": "results/.gross9-structural-clock-g9cb3-worker-a3dffd3cbec3afd582638a23",
  "status": "consumed_before_runtime_or_value_access",
  "token_sha256": "4c0366135814904429d72bbeb8562d042c74ebcd104fa663f1071b717bc4f555"
}
```

The sentinel and ledger status fields are true at their respective publication
boundaries. They are not final zero-access claims. The complete terminal
exposure finding is:

```text
worker capabilities consumed:
  pass 1 = 1
  pass 2 = 0
worker ledgers published:
  pass 1 = 1
  pass 2 = 0
declared isolated runtime roots imported = 2 of 2
market decoded and handed off = yes
funding decoded and handed off = yes
premium decoded and handed off = yes
open interest decoded and handed off = yes
exact decoded/handoff row counts recoverable = no
REX JSONL opened = no
Rank7 model/history opened = no
features constructed = no
schedules reached = no
sleeve intervals reached = no
candidate rows opened = 0
comparator clock rows opened = 0
pre-2025 anchor value rows opened = 0
portfolio return or PnL values computed = 0
CAGR or MDD values computed = 0
overlap metric values computed = 0
```

The following `G9CB-3` canonical publication paths are permanently absent:

```text
results/gross9_structural_clock_bundle_g9cb3_worker_capability_consumed_pass2_2026-07-31.json
results/gross9_structural_clock_bundle_g9cb3_2026-07-31.csv.gz
results/gross9_structural_clock_bundle_g9cb3_manifest_2026-07-31.json
```

The following transient products are also absent:

```text
results/.g9cb3-bytecode-cache-disabled
results/.gross9-structural-clock-g9cb3-worker-a3dffd3cbec3afd582638a23/gross9_structural_clock_bundle.csv.gz
results/.gross9-structural-clock-g9cb3-worker-a3dffd3cbec3afd582638a23/gross9_structural_clock_bundle_core.json
results/.gross9-structural-clock-g9cb3-worker-a3dffd3cbec3afd582638a23/gross9_structural_clock_bundle_pass_receipt.json
```

The exact `G9CB-3` stage states after failure are:

```text
slot 1:
  path =
    results/.gross9-structural-clock-g9cb3-worker-a3dffd3cbec3afd582638a23
  state = empty_directory
  filesystem_mode_octal = 0700
slot 2:
  path =
    results/.gross9-structural-clock-g9cb3-worker-26e64bf0a62646afad3d77e6
  state = absent
```

Later creation of a permanently absent `G9CB-3` path is terminal. Neither
stage is a canonical artifact. Neither may be committed, populated, removed,
renamed, reused, or treated as authority. The exact states must remain through
`T3` and `D4`.

All older `G9CB-2` terminal evidence, permanent absences, and residue states
frozen by the `G9CB-3` authority also remain unchanged through `D4`.
`G9CB-4` checks only its own stage prefix for active stages and separately
authenticates the preserved predecessor residue.

## T3 atomic terminal-evidence seal

Let `T3` be the direct child of `A4` that force-adds exactly the two
already-existing `G9CB-3` evidence files. `T3` is historical terminal-attempt
evidence, not `D3`.

Before `T3`, both files must reproduce the exact canonical bytes, fields,
sizes, SHA-256 values, prospective Git blobs, and filesystem modes above.
The sentinel internal manifest hash must reproduce exactly. The pass-2 ledger,
CSV, and manifest must remain absent. The two stage states and every older
`G9CB-2` residue state must remain unchanged.

`T3` must satisfy:

```text
T3 has exactly one parent
first_parent(T3) == A4
diff(A4, T3) ==
  A results/gross9_structural_clock_bundle_g9cb3_attempt_consumed_2026-07-31.json
  A results/gross9_structural_clock_bundle_g9cb3_worker_capability_consumed_pass1_2026-07-31.json
HEAD == T3 == @{upstream}
the worktree and index are clean
```

Git records both regular files as mode `100644`; both worktree files must
remain mode `0444`. Their committed Git blobs must be:

```text
sentinel = 672a973ae09495d38253991df90c7962c6f9c2ee
pass-1 ledger = 0e77d77fcdc70379d94a2c34e8dee19f9f45d0a9
```

Adding only one file is forbidden. Adding either file in another commit is
forbidden. No other `G9CB-3` output may be added at or after `T3`.

The `G9CB-4` failed-predecessor schema is widened atomically: a `G9CB-3`
predecessor row is valid only when it authenticates both the sentinel and the
pass-1 ledger and binds both to the same `T3`. Authentication of one without
the other is terminal. Neither file may be interpreted as a successful pass
receipt, per-pass core, CSV, manifest, or `D3`.

## Correct half-open value and boundary model

The economic domain remains exactly:

```text
DOMAIN_START = 2023-06-01T00:00:00Z
DOMAIN_END = 2026-06-01T00:00:00Z
step_seconds = 300
interval = [DOMAIN_START, DOMAIN_END)
```

`DOMAIN_END` is a boundary. It is not an authorized value-row timestamp.

The authenticated working market retains every inherited pre-domain warm-up
row needed by the frozen feature, model, and schedule logic. `G9CB-4` may
change the end cutoff to `exclude_from=DOMAIN_END`; it may not truncate the
working market at `DOMAIN_START` or otherwise remove warm-up history.

For the complete authenticated working market grid after the exact inherited
ordering and duplicate policy, let:

```text
value_opens = [v[0], v[1], ..., v[n-1]]
```

The value-open contract is:

```text
v[0] <= DOMAIN_START
DOMAIN_START is an element of value_opens
v[i + 1] == v[i] + 300 for every 0 <= i < n - 1
v[n - 1] + 300 == DOMAIN_END
v[i] < DOMAIN_END for every 0 <= i < n
```

The inherited raw duplicate policy remains exactly
`sort_values("date").drop_duplicates("date", keep="last")`. `G9CB-4` does not
turn a raw duplicate into a new error and does not change which raw row wins.
The uniqueness and complete-grid requirements below apply after that
normalization and to already-normalized synthetic/reference inputs.

The producer must reject:

- a missing value open;
- a duplicate that remains in a normalized or synthetic/reference grid;
- an off-grid value open;
- a missing aligned `DOMAIN_START` value open;
- an earlier final value open;
- a gap of any size;
- a value row whose open equals `DOMAIN_END`; and
- any other extra end-boundary value row.

The producer must not fabricate a market, funding, premium, open-interest,
feature, OHLC, prediction, model, label, or price value at `DOMAIN_END`.

The derived boundary vector is:

```text
boundaries = [v[0], v[1], ..., v[n-1], DOMAIN_END]
len(boundaries) == len(value_opens) + 1
```

The final element is geometry only. It is derived from the authenticated
300-second grid after validation. It is not a source row and must not
increment a decoded, handed-off, feature, model, OHLC, label, or source-value
counter.

The indexing contract is:

- value arrays, features, masks, predictions, OHLC, and source-backed labels
  have indices `0 .. n-1`;
- boundary-only exit geometry has indices `0 .. n`;
- entry times always index a physical value open;
- fixed exits use the boundary vector;
- barrier hits after value row `i` use boundary `i + 1`;
- a fixed or no-hit structural exit may equal boundary `n`, exactly
  `DOMAIN_END`;
- no feature, OHLC, open-price, model, label, or mask access may use index
  `n`; and
- no code path may use a value timestamp array as though it were the
  `n + 1`-element boundary vector.

For each inherited split `[split_start, split_end)`, define the existing
physical value mask:

```text
M[i] = split_start <= value_opens[i] < split_end, for 0 <= i < n
```

Schedule eligibility is exact:

- every signal and physical entry retains its inherited physical-row checks;
- for a fixed or no-hit exit boundary index `j < n`, eligibility remains
  exactly `M[j]`;
- for the sole derived terminal boundary index `j == n`, fixed or no-hit
  eligibility is
  `split_start <= boundaries[n] < split_end`;
- a barrier exit retains the inherited eligibility test on its occupied
  physical hit-row index `h`, exactly `M[h]`, and then emits boundary
  `h + 1`; and
- no other substitution of a boundary predicate for a physical-row mask is
  permitted.

Therefore a fixed exit at an interior split's exclusive end remains rejected
when that end has a physical row and `M[j]` is false. A barrier hit on the
last occupied row before an interior split end retains its inherited
acceptance and emits that split-end boundary. The derived `j == n` rule is
only the virtual equivalent of evaluating the inherited split predicate at
the global terminal boundary; it may not expand any interior schedule.

A structural geometry horizon equal to `len(value_opens)` is valid only when
the operation consumes no OHLC or open value at that horizon. A horizon
greater than `len(value_opens)` is invalid. Barrier scanning may consume the
existing rows in the half-open range before the horizon and then use the
derived boundary as the no-hit exit. It may not consume a fabricated cap row.

Rank7 or other economic label engines that require a cap or exit price remain
unchanged: they may use a horizon only when the required physical value
exists. This authority does not permit a derived boundary to substitute for a
price. It does not alter label construction, training history, source routing,
or economic semantics.

The direct and reference reconstruction paths must implement the same value
and boundary contract and produce byte-identical interval geometry. Any
out-of-range `mask`, `seconds`, value-array, or model-array indexing is
terminal.

## G9CB-4 identity and paths

The exact successor values are:

```text
identity = G9CB-4
preregistration protocol =
  gross9_structural_clock_bundle_g9cb4_preregistration_v1
builder/publication protocol =
  gross9_structural_clock_bundle_g9cb4_v1
terminal exception = TerminalG9CB4Failure
terminal action = TERMINAL_G9CB4_ATTEMPT_CONSUMED_NO_RETRY
bytecode prefix = results/.g9cb4-bytecode-cache-disabled
worker stage prefix = results/.gross9-structural-clock-g9cb4-worker-
```

The active paths are exactly:

| Role | Path |
|---|---|
| authority | `docs/gross9-structural-clock-bundle-g9cb4-successor-authority-decision-2026-07-31.md` |
| preregistration | `results/gross9_structural_clock_bundle_g9cb4_preregistration_2026-07-31.json` |
| access claim | `results/gross9_structural_clock_bundle_g9cb4_access_claim_2026-07-31.json` |
| attempt sentinel | `results/gross9_structural_clock_bundle_g9cb4_attempt_consumed_2026-07-31.json` |
| worker ledger pass 1 | `results/gross9_structural_clock_bundle_g9cb4_worker_capability_consumed_pass1_2026-07-31.json` |
| worker ledger pass 2 | `results/gross9_structural_clock_bundle_g9cb4_worker_capability_consumed_pass2_2026-07-31.json` |
| canonical CSV gzip | `results/gross9_structural_clock_bundle_g9cb4_2026-07-31.csv.gz` |
| final manifest | `results/gross9_structural_clock_bundle_g9cb4_manifest_2026-07-31.json` |

No older output path may be reused, replaced, renamed, deleted, or treated as
an alias.

## Parent and guarded-worker authentication

Before any predecessor or active bound-file read, the parent must:

1. establish the exact branch, clean index/worktree, pushed `HEAD`, ancestry,
   and commit topology;
2. enumerate every required predecessor path and classify its current
   `HEAD:path` and index Git pair;
3. reject any missing, additional, unmerged, intent-to-add, wrong-mode, or
   wrong-blob pair;
4. require each bound path to resolve uniquely to its canonical current path;
5. open each file once with no-follow semantics as a regular file;
6. authenticate its filesystem mode, exact bytes, size where bound, canonical
   JSON form, internal hash where defined, complete SHA-256, and derived Git
   blob from that one read; and
7. authenticate the complete predecessor history, permanent absences, and
   residue states before publishing the `G9CB-4` sentinel.

No two bound paths may alias the same current filesystem object. A symlink,
path substitution, second read, read-before-Git-classification, current
worktree/index drift, or disagreement among mode, SHA, blob, canonical hash,
internal hash, and commit binding is terminal.

The parent must authenticate all older `G9CB-1`, `G9CB-2`, and `G9CB-3`
bindings required by the inherited authorities. For `G9CB-3`, it must
authenticate the authority, Q3 protocol files, preregistration, claim,
sentinel, and pass-1 ledger, including the common `T3` seal, before reading
official source values.

The top-level `parent_authentication` object retains exactly these four keys:

```text
environment
hashed_inputs
preregistration_authentication
runtime_import_closure
```

No missing or additional key is allowed. The first, second, and fourth values
retain their inherited exact schemas. The
`parent_authentication.preregistration_authentication` value is exactly:

```json
{
  "manifest_hash": "<active preregistration manifest_hash>",
  "path": "results/gross9_structural_clock_bundle_g9cb4_preregistration_2026-07-31.json",
  "protocol_implementation_commit": "<exact Q4 commit>",
  "sha256": "<active preregistration complete-file SHA-256>"
}
```

The angle-bracket values are metavariables, not literal artifact strings. At
`P4` they are replaced by exact lowercase hashes and the exact 40-hex `Q4`
commit. The record has exactly those four string fields.

The complete parent-authentication object is serialized and hashed by the
unchanged canonical rule. The sentinel binds that SHA-256 before either worker
starts.

Before capability consumption, each guarded worker must validate the current
bytes and modes of the active preregistration and every required predecessor
binding without Git, subprocess, process creation, descriptor duplication, or
an external helper. It must:

- use the inherited stdlib-only guarded path;
- perform canonical no-symlink regular-file reads;
- authenticate mode `0444`, canonical JSON, complete-file SHA-256, internal
  hashes, identities, protocols, exact paths, and embedded bindings;
- authenticate the `G9CB-3` sentinel and pass-1 ledger as a mandatory pair;
- authenticate the predecessor permanent absences and residue disclosures;
- compare the active preregistration binding and `Q4` commit to the exact
  sentinel-bound parent-authentication record; and
- complete all inherited claim, sentinel, capability, runtime, and counter
  checks before source-value access.

The isolation guard remains installed at the earliest inherited bootstrap
point and unchanged in strength. Allowing Git or another child process in a
worker, moving the guard later, or treating official production as synthetic
is forbidden.

## Authority flow and protocol inventory

The active `G9CB-4` preregistration has one top-level
`authority_decision` binding with exactly:

```text
path
path_type
sha256
git_blob
git_mode
authority_commit
```

It binds this decision, its complete-file SHA-256, Git blob, mode `100644`,
regular-file type, and exact standalone `A4` commit. No other decision is the
active authority.

The inherited ordered `bindings.authority_amendments` list remains unchanged.
The `G9CB-4` `bindings.protocol` value is a sorted exact list of bindings for
these 16 paths:

```text
docs/gross9-structural-clock-bundle-authority-decision-2026-07-31.md
docs/gross9-structural-clock-bundle-g9cb3-successor-authority-decision-2026-07-31.md
docs/gross9-structural-clock-bundle-g9cb4-successor-authority-decision-2026-07-31.md
docs/gross9-structural-clock-bundle-preregistration-correction-amendment-2026-07-31.md
docs/gross9-structural-clock-bundle-rank7-authority-amendment-2026-07-31.md
docs/gross9-structural-clock-bundle-runtime-isolation-amendment-2026-07-31.md
docs/gross9-structural-clock-bundle-successor-authority-decision-2026-07-31.md
execution/gross9_rank7_clock_runtime.py
tests/test_build_gross9_structural_clock_bundle.py
tests/test_gross9_rank7_clock_runtime.py
tests/test_gross9_structural_clock_bundle_preregistration_artifact.py
tests/test_gross9_structural_clock_primitives.py
tests/test_preregister_gross9_structural_clock_bundle.py
training/build_gross9_structural_clock_bundle.py
training/gross9_structural_clock_primitives.py
training/preregister_gross9_structural_clock_bundle.py
```

Each row has exactly `path`, `path_type`, `sha256`, `git_blob`, and
`git_mode`; every path is a tracked mode-`100644` regular file at `Q4`.
Every digest and blob is derived from the exact current `Q4` bytes.

The three successor decisions remain in the inventory. This decision is
active. The `G9CB-2` and `G9CB-3` decisions are immutable predecessor
authorities.

The claim binds the complete active preregistration path, SHA-256, and
manifest hash. That binding carries the active decision, unchanged
amendments, exact protocol inventory, and all failed-predecessor evidence
through the sentinel, ledgers, per-pass cores, receipts, CSV, and final
manifest. Downstream artifacts do not add an alternate authority field.

## Closed failed-predecessor binding

The `G9CB-4` preregistration retains:

- the exact two failed `G9CB-1` preregistration bindings;
- the exact `G9CB-2` failed-predecessor-attempt row; and
- one new `G9CB-3` failed-predecessor-attempt row.

The inherited rows remain byte-for-byte semantically unchanged. The new row
must bind both `G9CB-3` evidence files. It may not copy the sentinel's
publication-time zero fields as final exposure.

In the closed schema below, `"<A4>"` and `"<T3>"` are metavariables. At `P4`
they are replaced by the exact lowercase 40-hex commits required by this
decision. Every other scalar is literal. No key may be added or omitted:

```json
{
  "access_claim": {
    "claim_hash": "886fa3b177cd6d48c630020c2afef7d85b05126fd3805676bffdbec2899f2c6a",
    "filesystem_mode_octal": "0444",
    "git_blob": "e4464c15f103c760a89ada18b0e49f7a874d8578",
    "git_mode": "100644",
    "path": "results/gross9_structural_clock_bundle_g9cb3_access_claim_2026-07-31.json",
    "path_type": "regular_file",
    "protocol_parent_commit": "7f4985b0685fdb6cbbde891485f08305f076e205",
    "seal_commit": "4365522eba5b01129c6aa12a5e06d314b611c840",
    "sha256": "6d59505ef14ce92bf5958d32725b7e5e1ffe8ce4065f215fdd092176a44bbe98"
  },
  "authority_decision": {
    "authority_commit": "a97576c050cf7cdf08738ddb755e63cc92484428",
    "git_blob": "43d68f6b7407c19b3b52ef8b7bb7010797dbf3b3",
    "git_mode": "100644",
    "path": "docs/gross9-structural-clock-bundle-g9cb3-successor-authority-decision-2026-07-31.md",
    "path_type": "regular_file",
    "sha256": "1df555c5149bfe269d2cc2c87375d54032809f13ac36f4e92b5ba00dd6e87cc7"
  },
  "classification": "terminal_missing_physical_domain_end_value_row_after_pass1_source_decode_before_features_or_model_access",
  "diagnostic": {
    "elapsed_seconds_approximate": "59.35",
    "max_rss_kb": 1352228,
    "parent_terminal_message": "fresh worker failed with PID 2087327 and status 1",
    "status": 1,
    "stderr_sha256": "6c3f838c364acda081f576f3d379c05c71ff8cce3c8424bb8de5c59d9e7e8d88",
    "stderr_size_bytes": 2894,
    "stdout_sha256": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
    "stdout_size_bytes": 0,
    "worker_terminal_message": "bound market lacks the canonical domain-end boundary row"
  },
  "exposure": {
    "candidate_rows_opened": 0,
    "comparator_clock_rows_opened": 0,
    "decoded_and_handed_off": [
      "market",
      "funding",
      "premium",
      "open_interest"
    ],
    "exact_decoded_and_handoff_counts_recoverable": false,
    "features_constructed": false,
    "isolated_runtime_roots_imported": 2,
    "overlap_metric_values_computed": 0,
    "portfolio_economic_values_computed": 0,
    "pre2025_anchor_value_rows_opened": 0,
    "rank7_model_or_history_opened": false,
    "rex_jsonl_opened": false,
    "schedules_reached": false,
    "sleeve_intervals_reached": false,
    "worker_capabilities_consumed": {
      "pass1": 1,
      "pass2": 0
    },
    "worker_ledgers_published": {
      "pass1": 1,
      "pass2": 0
    }
  },
  "identity": "G9CB-3",
  "permanently_absent_outputs": [
    "results/gross9_structural_clock_bundle_g9cb3_worker_capability_consumed_pass2_2026-07-31.json",
    "results/gross9_structural_clock_bundle_g9cb3_2026-07-31.csv.gz",
    "results/gross9_structural_clock_bundle_g9cb3_manifest_2026-07-31.json"
  ],
  "preregistration": {
    "filesystem_mode_octal": "0444",
    "git_blob": "e4b2488783f0cfa88a8478b3ff0d94c99fbece6e",
    "git_mode": "100644",
    "manifest_hash": "e100e54557a9654923374dec433feff494d85b0956129157231c2fb8551ec5f2",
    "path": "results/gross9_structural_clock_bundle_g9cb3_preregistration_2026-07-31.json",
    "path_type": "regular_file",
    "protocol_implementation_commit": "211752bf2529b37376b5fecdc190e0c22a1195ff",
    "protocol_version": "gross9_structural_clock_bundle_g9cb3_preregistration_v1",
    "seal_commit": "7f4985b0685fdb6cbbde891485f08305f076e205",
    "sha256": "1d967a590e50822dd83c20838c8796523d6594f0fcbd22aad137b0a97c1a982c"
  },
  "protocol_implementation": {
    "commit": "211752bf2529b37376b5fecdc190e0c22a1195ff",
    "files": [
      {
        "git_blob": "ce6fec59bd8c81dcef4a1be8c04d0b6e8457aa2c",
        "git_mode": "100644",
        "path": "tests/test_build_gross9_structural_clock_bundle.py",
        "sha256": "b59b808b3fcd55aac3278f6914e3022d91688880bb605e8fe635543028438a77"
      },
      {
        "git_blob": "3158c711293fdb110f0a27b960aa0fa060eda9f6",
        "git_mode": "100644",
        "path": "tests/test_gross9_structural_clock_bundle_preregistration_artifact.py",
        "sha256": "19add878f1171aa9d22f18f7547cb2d0274d8c90666bcd812d7c7e1c40769a8c"
      },
      {
        "git_blob": "e63a1578f434d716adc260c7d26c433ce8c5dcab",
        "git_mode": "100644",
        "path": "tests/test_preregister_gross9_structural_clock_bundle.py",
        "sha256": "6487f6f4601e8e137846c9504373e2e76247d321c1e5b24e1e2340e1b151e67a"
      },
      {
        "git_blob": "0e5b96802affa0ab221f75ac1387fd84fec2867a",
        "git_mode": "100644",
        "path": "training/build_gross9_structural_clock_bundle.py",
        "sha256": "50cece625b4af12f81842ba501d376ed0c50589d33187b54efb2139194000e27"
      },
      {
        "git_blob": "a11e63d31e37ab391051bb456091401577f43b2f",
        "git_mode": "100644",
        "path": "training/preregister_gross9_structural_clock_bundle.py",
        "sha256": "71415da68e4a0a37cc2ff1d562f9014f74c297f62124cacba43cdf4ab375b113"
      }
    ],
    "parent_commit": "edad4de5cf5524c4646c64b0581e47c914e31425"
  },
  "protocol_version": "gross9_structural_clock_bundle_g9cb3_v1",
  "residue": {
    "bytecode_cache": {
      "path": "results/.g9cb3-bytecode-cache-disabled",
      "state": "absent"
    },
    "slot1_stage": {
      "committed": false,
      "filesystem_mode_octal": "0700",
      "path": "results/.gross9-structural-clock-g9cb3-worker-a3dffd3cbec3afd582638a23",
      "staged_core_state": "absent",
      "staged_csv_state": "absent",
      "staged_receipt_state": "absent",
      "state": "empty_directory"
    },
    "slot2_stage": {
      "committed": false,
      "path": "results/.gross9-structural-clock-g9cb3-worker-26e64bf0a62646afad3d77e6",
      "state": "absent"
    }
  },
  "root_cause": {
    "domain_end": "2026-06-01T00:00:00Z",
    "domain_end_is_exclusive_boundary": true,
    "fabricated_boundary_value_authorized": false,
    "open_interest_left_merge_can_remove_market_rows": false,
    "source_hash_mismatch_at_authentication_ruled_out": true,
    "wrong_requirement": "exclusive domain-end boundary required as physical market value-row open"
  },
  "status": "historical_terminal_attempt_consumed_no_clock_authority",
  "terminal_evidence": {
    "attempt_sentinel": {
      "claim_commit": "4365522eba5b01129c6aa12a5e06d314b611c840",
      "filesystem_mode_octal": "0444",
      "git_blob": "672a973ae09495d38253991df90c7962c6f9c2ee",
      "git_mode": "100644",
      "manifest_hash": "aab875ecc91a07619b3006967efca42664cc77d90d0bb9e45da20e93ea16c973",
      "path": "results/gross9_structural_clock_bundle_g9cb3_attempt_consumed_2026-07-31.json",
      "path_type": "regular_file",
      "protocol_version": "gross9_structural_clock_bundle_g9cb3_v1",
      "resume_allowed": false,
      "retry_allowed": false,
      "seal_commit": "<T3>",
      "sha256": "7cec9807503c271e993f8bf456ca772d7ded9cec9619d37cbd4ac015d14c9f69",
      "size_bytes": 3300,
      "status": "attempt_consumed_before_runtime_or_value_access"
    },
    "pass1_worker_ledger": {
      "filesystem_mode_octal": "0444",
      "git_blob": "0e77d77fcdc70379d94a2c34e8dee19f9f45d0a9",
      "git_mode": "100644",
      "path": "results/gross9_structural_clock_bundle_g9cb3_worker_capability_consumed_pass1_2026-07-31.json",
      "path_type": "regular_file",
      "seal_commit": "<T3>",
      "sha256": "45b27b24074c53eeaaa9a1074c7888c22c4078e5fb5922007d31ad0baf704f66",
      "size_bytes": 1766,
      "slot": 1,
      "status": "consumed_before_runtime_or_value_access"
    }
  },
  "topology": {
    "g9cb3_authority_commit": "a97576c050cf7cdf08738ddb755e63cc92484428",
    "g9cb3_claim_commit": "4365522eba5b01129c6aa12a5e06d314b611c840",
    "g9cb3_preregistration_commit": "7f4985b0685fdb6cbbde891485f08305f076e205",
    "g9cb3_protocol_commit": "211752bf2529b37376b5fecdc190e0c22a1195ff",
    "g9cb4_authority_commit": "<A4>",
    "terminal_evidence_commit": "<T3>"
  }
}
```

The producer and parent must authenticate the complete row with Git before any
bound-file read. The guarded worker must authenticate the same embedded row
from current bytes and modes plus the sentinel-bound parent authentication,
without Git or subprocess.

## Exact commit topology and diffs

Every seal uses:

```text
codex/gross9-structural-clock-bundle-20260731
```

Let:

- `C3` be `4365522eba5b01129c6aa12a5e06d314b611c840`;
- `A4` add only this decision;
- `T3` add only the immutable `G9CB-3` sentinel and pass-1 ledger;
- `Q4` be the clean pushed `G9CB-4` protocol implementation;
- `P4` add only the `G9CB-4` preregistration;
- `C4` add only the `G9CB-4` claim; and
- `D4` add only the five successful `G9CB-4` publication files.

`A4` must satisfy:

```text
A4 has exactly one parent
first_parent(A4) == 4365522eba5b01129c6aa12a5e06d314b611c840
diff(C3, A4) ==
  A docs/gross9-structural-clock-bundle-g9cb4-successor-authority-decision-2026-07-31.md
HEAD == A4 == @{upstream}
the worktree and index are clean
```

`T3` must satisfy the exact atomic terminal-evidence rules above.

`Q4` must satisfy:

```text
Q4 has exactly one parent
first_parent(Q4) == T3
diff(T3, Q4) ==
  M tests/test_build_gross9_structural_clock_bundle.py
  M tests/test_gross9_structural_clock_bundle_preregistration_artifact.py
  M tests/test_preregister_gross9_structural_clock_bundle.py
  M training/build_gross9_structural_clock_bundle.py
  M training/preregister_gross9_structural_clock_bundle.py
HEAD == Q4 == @{upstream}
the worktree and index are clean
```

Those five paths are exact, not illustrative. `Q4` may change only:

- identity, protocol, path, exception, action, bytecode, and stage-prefix
  names;
- authority and exact topology/history bindings;
- the two-evidence `G9CB-3` failed-predecessor schema and exposure disclosure;
- authentication needed to enforce those bindings;
- the separate value-open and derived-boundary implementation;
- safe structural `horizon == len(value_opens)` geometry where no horizon
  value is consumed;
- prevention of every out-of-range boundary/value/mask/model access; and
- tests for these exact changes.

`Q4` may not alter a source path or hash, official source byte, domain endpoint,
signal, model, training history, feature, schedule, sleeve, weight, side, hold,
barrier, entry rule, stop-before-take order, Rank7 policy, economic label
engine's need for a physical cap price, counter event, dependency, environment,
runtime import closure, capability, guard strength, two-pass rule, serializer,
publication algorithm, economic prohibition, or overlap prohibition.

No `G9CB-4` artifact may exist at `Q4`.

`P4` must satisfy:

```text
P4 has exactly one parent
first_parent(P4) == Q4
diff(Q4, P4) ==
  A results/gross9_structural_clock_bundle_g9cb4_preregistration_2026-07-31.json
HEAD == P4 == @{upstream}
the worktree and index are clean
```

`C4` must satisfy:

```text
C4 has exactly one parent
first_parent(C4) == P4
diff(P4, C4) ==
  A results/gross9_structural_clock_bundle_g9cb4_access_claim_2026-07-31.json
HEAD == C4 == @{upstream}
the worktree and index are clean
```

`D4` must satisfy:

```text
D4 has exactly one parent
first_parent(D4) == C4
diff(C4, D4) ==
  A results/gross9_structural_clock_bundle_g9cb4_2026-07-31.csv.gz
  A results/gross9_structural_clock_bundle_g9cb4_attempt_consumed_2026-07-31.json
  A results/gross9_structural_clock_bundle_g9cb4_manifest_2026-07-31.json
  A results/gross9_structural_clock_bundle_g9cb4_worker_capability_consumed_pass1_2026-07-31.json
  A results/gross9_structural_clock_bundle_g9cb4_worker_capability_consumed_pass2_2026-07-31.json
HEAD == D4 == @{upstream}
the worktree and index are clean
```

No authority, predecessor evidence, protocol, test, preregistration, or claim
byte may change between `P4`, `C4`, and `D4`.

## Required regression evidence

Before `Q4` is sealed, synthetic tests must prove at least:

1. the exact `C3 -> A4 -> T3 -> Q4 -> P4 -> C4 -> D4` topology and exact
   per-commit diffs;
2. `T3` authenticates and commits the `G9CB-3` sentinel and pass-1 ledger
   together, rejects either one alone, and never classifies them as `D3`;
3. the predecessor exposure disclosure preserves publication-time zero fields
   as publication-time facts while separately recording post-ledger imports,
   decodes, handoffs, unopened inputs, unreached computations, and
   unrecoverable exact counts;
4. all `G9CB-2` and `G9CB-3` permanent absences and residue states remain
   exact through `D4`;
5. parent authentication validates every predecessor current Git pair before
   its one no-follow read and rejects path aliasing, mode drift, byte drift,
   canonical drift, internal-hash drift, SHA drift, blob drift, topology
   drift, history drift, absence drift, and residue drift;
6. guarded-worker authentication validates current bytes and modes, including
   both `G9CB-3` evidence files, before capability consumption without Git,
   subprocess, process creation, descriptor duplication, or an external
   helper;
7. an aligned half-open synthetic grid whose last physical value open is
   exactly `DOMAIN_END - 300 seconds` succeeds;
8. a physical value row at `DOMAIN_END` is rejected;
9. an earlier final row, any missing normalized row or gap, a duplicate in an
   already-normalized reference grid, and an off-grid timestamp are each
   rejected, while raw duplicates preserve the inherited sorted
   `keep="last"` resolution;
10. fixed and barrier structural exits may equal `DOMAIN_END` only through the
    derived boundary vector;
11. structural geometry with `horizon == len(value_opens)` is safe when no
    horizon OHLC/open value is consumed, while `horizon > len(value_opens)` is
    rejected;
12. the derived end boundary never enters feature, OHLC, open-price, source,
    mask, prediction, model, Rank7 label, or economic value arrays and never
    increments a value-use counter;
13. Rank7 and economic label engines still reject a cap that lacks a physical
    price value;
14. every mask, seconds, boundary, value, and model index remains in its
    authorized range;
15. direct and reference implementations have exact interval and counter
    parity on boundary-ending fixed and barrier cases, and an interior
    split-boundary regression proves that fixed and barrier eligibility
    retains the exact predicate above without schedule expansion;
16. the `G9CB-4` stage prefix is isolated from all preserved `G9CB-2` and
    `G9CB-3` residue, and no old stage can block, satisfy, or alias a
    `G9CB-4` stage;
17. all previous authentication regressions remain passing;
18. the frozen inherited baseline of 194 tests remains passing, with new
    `G9CB-4` tests added rather than substituted for that baseline;
19. the active preregistration reproduces byte-for-byte at `P4` and `D4`; and
20. committed-publication validation authenticates every `G9CB-4` artifact at
    `D4`.

Tests may use synthetic bytes, timestamps, frames, and Git repositories. They
must not read or decode official source values, model values, or history
values, and must not compute portfolio economics.

## Completion and terminal rule

`G9CB-4` is complete only when `A4`, `T3`, `Q4`, `P4`, `C4`, and `D4`
satisfy their exact clean-pushed topology; exactly two fresh workers produce
byte-identical authenticated outputs; the final manifest is published last;
the committed-publication verifier passes at `D4`; all predecessor residue
remains exact; and the worktree remains clean with
`HEAD == @{upstream}`.

The official `G9CB-4 --produce` command may be invoked exactly once and only
after clean pushed `C4`. No official-source rehearsal, probe, decode, partial
run, retry, or second invocation is authorized.

Before the `G9CB-4` sentinel, any non-restorable protocol failure closes
`G9CB-4` without mutation under the same identity. After the sentinel, every
failure is terminal:

```text
TERMINAL_G9CB4_ATTEMPT_CONSUMED_NO_RETRY
```

After any `G9CB-4` sentinel is published, every retry, resume, repair,
amendment, protocol mutation, metadata correction, second production
invocation, v2-under-`G9CB-4`, or completion attempt under the same identity
is forbidden. Any future successor requires another new infrastructure
identity and standalone decision.

This decision authorizes no alpha candidate, official source-value
exploration, candidate ranking, portfolio return, PnL, funding cash, CAGR,
MDD, economic metric, or overlap metric. Those remain forbidden until
`G9CB-4` has a clean, pushed, verified `D4` publication and a separate later
candidate preregistration authorizes the next access.
