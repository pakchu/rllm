# CLDCR-6 preregistration

CLDCR-6 is frozen before exact incidence, Gross9 clocks, execution prices, funding, or post-entry outcomes are opened. At canonical heights divisible by 36 it computes six BTC perpetual log returns over the consecutive block-confirmation intervals through H+6. The cumulative absolute return must newly enter its strict-prior q90 tail and at least five of six interval returns must share the cumulative strict sign.

After six confirmations and a conservative prefix-maximum header-time plus two-hour embargo, the unchanged policy follows that sign from the next five-minute open for six elapsed hours at fixed 0.5 gross. This is ordered price coherence on external block time, not fixed-clock sign persistence, a wall-clock breakout, range/variance ratio, volume, flow, funding, premium, OI, block throughput, CLCS source substitution, or a prior diagnostic. RV20 q90 remains a later audit only and every gate is terminal.
