# AVFCR-12 preregistration

AVFCR-12 is frozen before source incidence, Gross9 clocks, execution prices,
or post-entry outcomes are opened. At each UTC day boundary it aggregates the
completed final six hours of BTCUSDT one-minute bars, reconstructs aggressive
buy VWAP and residual aggressive sell VWAP from their separate base and quote
notionals, and compares their log separation with aggregate signed taker flow.
When the two signs oppose, it follows the execution-price-dominant side from
00:05 UTC for twelve elapsed hours at fixed 0.5 gross.

This is a minority-aggressor latent-impact mechanism, not aggregate flow
confirmation, close-to-VWAP reversion, liquidity impact per volume, return
trend, funding, or volatility entry gating. RV20 q90 remains a later audit
against an identical-clock forced-long comparator. Every source, novelty, and
economic gate is terminal with no formula, contradiction, side, clock, hold,
subset, control, comparator, or threshold repair.
