# HVTPDCR-8 Gross9 novelty pass

The unchanged 138-row `HVTPDCR-8` clock passed every frozen Gross9 structural
novelty limit before any BTC execution price, return, funding row, or economic
metric was opened.

- Exact-entry Jaccard: `0.0` against every sleeve.
- Worst one-to-one ±6h matched share: `0.1471` (`<=0.35`).
- Worst occupied-5m Jaccard: `0.0626` (`<=0.25`).
- Worst absolute signed-exposure correlation: `0.0542` (`<=0.35`).

The source-support predecessor was not mutated. Economic access is authorized
only for the unchanged candidate under the frozen sequential accounting rules.
